const routes = {
  home: "/",
  menu: "/menu",
  searchAll: "/search",
  books: "/book",
  device: "/device",
  manager: "/manager",
  detail: "/detail",
  myPage: "/mypage",
};

Object.freeze(routes);
export default routes;
