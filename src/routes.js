const routes = {
  home: "/",
  menu: "/menu",
  searchAll: "/search",
  books: "/books",
  device: "/device",
  manager: "/manager",
  detail: "/detail",
  myPage: "/my-page",
};

Object.freeze(routes);
export default routes;
